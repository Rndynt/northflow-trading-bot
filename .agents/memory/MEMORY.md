- [Phase 1 Core Domain](phase1-core-domain.md) — Phase 1 complete: all core types in src/core/, 61 tests pass, CLI prints status.
- [Phase 2 Market Data design](phase2-market.md) — OhlcvLoader returns OhlcvLoadResult (never panics); all CSV errors in DataQualityReport; MissingCandleGap is warning not error; IrregularInterval (delta < 60_000 ms) is an error.
- [Timeframe builder bucket rule](timeframe-builder-rule.md) — builds 5m/15m directly from 1m only; bucket must have EXACTLY required count (!=, not <); overfilled AND underfilled buckets dropped silently; BTreeMap gives deterministic sorted iteration.
- [Timestamp normalisation threshold](timestamp-norm.md) — strict i64 only (no f64 fallback); decimal/NaN/inf/negative/zero all rejected; values < 10^12 → seconds × 1000; values ≥ 10^12 kept as milliseconds.
- [src/data deprecation](data-deprecation.md) — src/data/mod.rs kept on disk but removed from pub mod in lib.rs; active loader is crate::market::OhlcvLoader only.
- [Phase 3 Indicators design](phase3-indicators.md) — EMA/ATR/VWAP/VolumeSma all use Result API; NorthflowError::ConfigError for period==0, DataError for bad values; ATR uses Wilder warmup (collect N TRs → mean) then ((prev*(N-1))+tr)/N; VWAP zero-vol returns current value not None; IndicatorSnapshot+IndicatorEngine in snapshot.rs; 173 tests pass.
- [Phase 4 Strategy design](phase4-strategy.md) — traits.rs/regime.rs/screened_vwap_scalp.rs; 20 bps near-boundary must use close=5000/ema=4990 (delta=10.0 exact) because 100.0-99.8 gives >20.0 in IEEE 754; 216 tests pass; all 3 input candles validated at top of evaluate() — invalid candle → Err, missing indicators → Ok(None).
- [Phase 5 Risk design](phase5-risk.md) — position_sizing/cost_model/guard in src/risk/; stop_slippage_bps added to ResearchConfig (default 5.0); RiskEngine::assess() returns Ok(assessment) for normal rejection, Err only for invalid input; cost uses take_profit as exit_price; 251 tests pass.
- [Phase 6 Backtest Engine design](phase6-backtest.md) — 6 files in src/backtest/; Trade gains regime:String; ResearchConfig gains max_bars_held:u32 (default 60); temp_dir race in tests fixed with per-test tags; walk_forward total==train+test → 1 window not 0; 306 tests pass.
- [Phase 6 Hardening rules](phase6-hardening.md) — RiskEngine Err must propagate (not swallow); same-candle exit enabled via entered_this_bar flag (remove continue after pending entry); try_assess_risk() removed — call RiskEngine::assess directly in each branch; fill_model check_exit works at bars_held=0.
- [Phase 7 Reports design](phase7-reports.md) — 4 files in src/report/; `gen` is reserved in Edition 2024 (use `generated_by`); audit.passed=false only on Errors (warnings don't block); filter keys: "passed:<name>"/"failed:<name>"; manifest uses BTreeMap for sorted paths; 360 tests pass.
- [Realism patch — risk attribution](risk-attribution-patch.md) — pending_entry is Option<Signal>; actual entry re-risks at adverse open price; invalid geometry → soft rejection; ManifestWriter::build gains risk_rejections_count:usize; 384 tests after realism patch.
- [Correctness patch — stage + assessment fields](correctness-patch-stage.md) — RiskRejection gains stage:String ("initial_risk"/"actual_entry"); build_rejection() stage is 2nd param; normal Ok(assessment) if !approved rejections use assessment.expected_* fields not signal fields; CSV header: signal_id,stage,timestamp,...; 393 tests pass.
- [Entry geometry mode patch](entry-geometry-mode.md) — EntryGeometryMode enum in src/backtest/geometry.rs; ResearchConfig stores as String (no circular dep); parsed in BacktestEngine::run via EntryGeometryMode::parse (fails on unknown); build_rejection gains entry_geometry_mode 3rd param; 411 tests pass.
- [Strategy diagnostic patch](diagnostic-patch.md) — src/report/diagnostics.rs; month_key_from_ms uses div_euclid+Hinnant; profit_factor accumulates gross_losers from ALL net-losing trades (not just gross-negative); ManifestWriter::build gains DiagnosticReport 6th param; write_all_with_trades is the primary entry point; 441 tests pass.
- [V2 strategy patch](v2-strategy-patch.md) — screened_vwap_scalp_v2 in src/strategy/; V2Config in src/config/mod.rs; ActiveStrategy enum in engine; cooldown via last_signal_bar usize; validate_strategy_config before CSV check; attribution_by_strategy.csv; 478 tests pass.
